# ─── Terraform ─────────────────────────────────────────────
.terraform/                  # Configuración local de Terraform
*.tfstate                    # Archivos de estado
*.tfstate.*                  # Backups de estado
.terraform.lock.hcl          # Lock de dependencias
crash.log                    # Logs de errores de Terraform
override.tf
override.tf.json
*_override.tf
*_override.tf.json
terraform/*.tfvars           # Variables sensibles

# Ignorar también cualquier tfvars global fuera de /terraform
*.tfvars

# ─── Docker ────────────────────────────────────────────────
*.log                        # Logs generados por contenedores
docker-compose.override.yml  # Overrides locales (opcional)

# ─── IDE / Editor ──────────────────────────────────────────
.vscode/
.idea/
*.swp
*.swo
*~
*.code-workspace

# ─── Sistema operativo ─────────────────────────────────────
.DS_Store
Thumbs.db

# ─── AWS / Credenciales ────────────────────────────────────
aws-config.txt
credentials.txt
.aws/

clave-nueva.json
